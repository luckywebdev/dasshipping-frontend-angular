// import { TextColorDirective } from './text-color.directive';

describe('TextWeightDirective', () => {
  it('should create an instance', () => {
    // const directive = new TextColorDirective();
    // expect(directive).toBeTruthy();
  });
});
