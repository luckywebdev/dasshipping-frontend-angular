export interface FilterDTO {
  origin?: string;
  destination?: string;
  vehicleType?: string;
  carType?: string;
  minimumNumberOfVehiclesPerLoad?: string;
  trailerType?: string;
  condition?: string;
  makeOrModel?: string;
}
